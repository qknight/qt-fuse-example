# qt-fuse

simplest way to integrate the fuse eventloop (fuse_loop()/fuse_loop_mt()) into Qt 4.x using a QThread. 

see the qt-fuse/README for more details

# qt-fuse-integrated

example how to integrate the FUSE eventloop (fuse_loop()/fuse_loop_mt()) into the Qt eventloop using QLocalSocket and QThreads.

see the qt-fuse-integrated/README for more details
